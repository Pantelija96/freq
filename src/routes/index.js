// src/routes/index.js
const express = require('express');
const router = express.Router();

// We will mount other routers here later
// For now it's empty - we will add them step by step

module.exports = router;
